export enum ComponentType {
  Default,
  PositionComponent,
  NameComponent,
  HealthComponent,
  InventoryComponent,
  PlayableComponent,
}

export enum EventType {
  Keyboard,
  Mouse,
}

export enum GameState {
  Menu,
  Running,
  Pause,
}
